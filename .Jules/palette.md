## 2024-05-18 - Init\n**Learning:** Accessibility matters\n**Action:** Add aria-labels

## 2025-05-18 - Improve Semantic Buttons & Aria Labels
**Learning:** Icon-only interactive elements must use semantic `<button>` tags and include `aria-label` attributes to ensure they are fully accessible via keyboard navigation and screen readers.
**Action:** Converted `div.dock-item` to `<button>` elements and added appropriate `aria-label` attributes to all icon-only buttons including chat controls and dock items.
